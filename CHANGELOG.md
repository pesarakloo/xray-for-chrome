# تغییرات

## 0.3.9

- اصلاح ساختار کانفیگ خروجی VLESS با استفاده از `settings.vnext` و `users`
- اصلاح ساختار کانفیگ خروجی VMess AEAD با استفاده از `settings.vnext`، `users` و `alterId: 0`
- اصلاح ساختار کانفیگ خروجی Shadowsocks با استفاده از `settings.servers`
- اصلاح تعیین نوع انتقال در کانفیگ Xray با استفاده از `streamSettings.network`
- تنظیم دانلود خودکار نصب‌کننده Windows روی نسخه `v25.8.3` هسته Xray
- حفظ هسته Xray موجود هنگام نصب مجدد در حالت عادی، بدون دانلود غیرضروری
- افزودن گزینه `-ForceDownload` برای دانلود مجدد هسته و حفظ امکان نصب با فایل محلی از طریق `-XrayExe`
- بهبود دریافت فایل در Windows PowerShell 5.1 با `-UseBasicParsing`، نمایش مراحل نصب و پیام‌های خطای روشن‌تر
- تعیین مهلت پیش‌فرض ۳۰ ثانیه برای دریافت اطلاعات انتشار و ۱۸۰ ثانیه برای دانلود؛ قابل تنظیم با `-MetadataTimeoutSec` و `-DownloadTimeoutSec`
- نمایش مسیر دقیق پوشه `extension` در پایان نصب Windows برای انتخاب در `Load unpacked`
- افزودن تب «آموزش اتصال» با راهنمای جداگانه Windows و macOS، نصب برنامه مکمل، افزودن کانفیگ و سابسکریپشن، تست اتصال و رفع خطا
- افزودن دکمه کپی دستورهای نصب و حذف برنامه مکمل در تب آموزش
- توضیح تفاوت نصب افزونه از Chrome Web Store و بارگذاری دستی نسخه GitHub در راهنما
- افزودن لینک وب‌سایت [آنی‌سافت](https://anisoft.ir) به بخش «درباره ما»
- فشرده‌سازی چیدمان «درباره ما»، کاهش فاصله‌های خالی و اصلاح ارتفاع صفحه با حفظ تم تیره
- هماهنگ‌کردن شماره نسخه در `manifest.json` و `package.json` روی `0.3.9`
- حفظ شناسه ثابت افزونه برای ادامه استفاده از کانفیگ‌های ذخیره‌شده هنگام به‌روزرسانی بدون حذف افزونه
- به‌روزرسانی README و راهنمای نصب، استفاده و عیب‌یابی

**راهنمای ارتقا:** ابتدا اتصال را قطع کنید، فایل‌های نسخه جدید را در مسیر قبلی جایگزین کنید، نصب‌کننده برنامه مکمل سیستم‌عامل خود را دوباره اجرا کنید و سپس افزونه را در `chrome://extensions` با **Reload** به‌روزرسانی کنید. حذف افزونه لازم نیست.

**هسته Windows:** در نصب مجدد عادی، هسته موجود حفظ می‌شود؛ گزینه `-ForceDownload` نسخه `v25.8.3` را دوباره دانلود می‌کند و به معنی دریافت جدیدترین نسخه Xray نیست.

## 0.3.3 — 2026-09-24

- افزودن تب «درباره ما» با لینک‌های [گیت‌هاب](https://github.com/pesarakloo) و [تلگرام](https://t.me/xray_chrome)
- حفظ تم تیره، رنگ‌بندی و کارت‌های رابط فارسی
- نمایش پینگ HTTPS هر کانفیگ در فهرست مدیریت، منوی انتخاب و صفحه اتصال
- تست تکی و گروهی با حداکثر دو اجرای هم‌زمان، توقف صف و ذخیره زمان آخرین نتیجه
- اندازه‌گیری مستقل از اتصال فعال با Xray موقت روی پورت Loopback جدا در Windows و macOS
- مدیریت Timeout، کانفیگ نامعتبر، حذف پروفایل حین تست و برنامه همراه قدیمی
- افزودن قابلیت تست پینگ به برنامه‌های همراه Windows و macOS
- به‌روزرسانی تست قدیمی WMI برای هماهنگی با اجرای مستقیم پردازش در Windows

برای فعال‌شدن پینگ، برنامه همراه این بسته باید دوباره نصب و افزونه Reload شود.

## 0.3.1

- حذف استفاده از WMI برای ایجاد پردازش Xray در Windows
- به‌روزرسانی راه‌اندازی هسته با اجرای مستقیم پردازش در برنامه همراه Windows
- حفظ تغییرات Parser و پردازش پارامترهای کانفیگ

## 0.3.0 — 2026-09-24

- تغییر راه‌اندازی Xray در Windows برای جلوگیری از بسته‌شدن آن پس از پایان پیام Native Messaging، با استفاده از WMI در این نسخه
- افزودن پردازش پارامترهای لینک اشتراک‌گذاری 3x-ui برای XHTTP، TLS، REALITY، FinalMask و mKCP
- تبدیل `headerType` و `seed` قدیمی mKCP به `finalmask.udp` از نوع `mkcp-legacy`
- افزودن پردازش `obfs-local;obfs=http` در لینک Shadowsocks
- افزودن نگاشت پارامترهای پیشرفته TLS و REALITY به کانفیگ خروجی
- اعتبارسنجی ترکیب REALITY و نوع انتقال و حالت XHTTP پیش از اجرای هسته
- افزودن تست‌های VMess gRPC، XHTTP، FinalMask، TLS پیشرفته و چرخه اجرای برنامه همراه Windows

## 0.2.1 — 2026-09-24

- رفع خطای Parse نصب‌کننده در Windows PowerShell 5.1 با حذف وابستگی متن اسکریپت به کدگذاری فارسی
- مشخص‌کردن UTF-8 برای کامپایل برنامه همراه Windows
- حذف وابستگی macOS به Xcode، Swift و Command Line Tools
- جایگزینی برنامه همراه macOS با نسخه‌ای بر پایه ابزارهای داخلی سیستم
- دانلود بسته Xray متناسب با معماری Apple Silicon یا Intel

## 0.2.0 — 2026-09-24

- ارائه یک افزونه مشترک برای Windows و macOS
- تفکیک برنامه‌های همراه در `native-hosts/windows` و `native-hosts/macos`
- ارائه نخستین Native Host بومی macOS با Swift
- نصب خودکار Xray برای Apple Silicon و Intel
- ثبت Native Messaging در مسیر کاربر Google Chrome روی macOS
- بررسی SHA-256 بسته Xray در صورت ارائه digest رسمی
- نصب و حذف مستقل برنامه همراه برای هر سیستم‌عامل

## 0.1.0 — 2026-09-24

- انتشار نسخه اولیه Windows
- افزونه Chrome Manifest V3 با رابط فارسی
- افزودن ورود لینک‌های VLESS، VMess AEAD، Shadowsocks و Trojan
- افزودن Subscription و مدیریت کانفیگ‌ها
- برنامه همراه Native Messaging برای اجرای Xray
- اعمال پراکسی در Chrome با اتصال محلی Loopback
- محدودسازی WebRTC غیرپراکسی هنگام اتصال و نمایش گزارش خطا
- نصب برنامه همراه در حساب کاربری بدون نیاز به Administrator

